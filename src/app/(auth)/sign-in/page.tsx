import React from 'react'

const page = () => {
  //over here also we will keep the same logic as the sign-in page
  //same onsubmit and then the data cominf from the form and then we will send the data to the backend
  //but here we will be sending the username and the code to the backend
  return (
    <div>page</div>
  )
}

export default page